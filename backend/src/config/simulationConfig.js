export const DEFAULT_MAX_DAYS = 500;

export function parseMaxDays(value) {
  const numericValue = Number(value);

  if (!Number.isInteger(numericValue) || numericValue <= 0) {
    return null;
  }

  return numericValue;
}
